#include <stdio.h>
#include <stdlib.h>
/**
 * add - a function that sums two integers
 * @a: first integer
 * @b: second integer
 * Return: sum of a and b
 */
int sum(int a, int b)
{
	return (a + b);
}
