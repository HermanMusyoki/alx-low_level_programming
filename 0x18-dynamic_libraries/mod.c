#include <stdio.h>
#include <stdlib.h>
/**
 * add - a function that finds the reminder of two integers
 * @a: first integer
 * @b: second integer
 * Return: reminder d disivion of a and b
 */
int mod(int a, int b)
{
	return (a % b);
}
